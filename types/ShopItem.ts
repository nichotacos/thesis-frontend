export type ShopItem = {
    _id: string;
    name: string;
    description: string;
    image: string;
    price: number;
    category: string;
    active: boolean;
    createdAt: string;
    updatedAt: string;
}